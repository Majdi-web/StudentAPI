apiVersion: v1
kind: Service
metadata:
  name: {{ template "kubernetes8.fullname" . }}
  labels:
    app: {{ template "kubernetes8.name" . }}
    chart: {{ template "kubernetes8.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "kubernetes8.name" . }}
    release: {{ .Release.Name }}
